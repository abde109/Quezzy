{
  "name": "quizmaster-app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run serve --prefix backend\" \"npm run front --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
